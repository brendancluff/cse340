<%- include('partials/header') %>

<main>
  <h1><%= title %></h1>

  <p>
    <a href="/new-organization">Add a New Organization</a>
  </p>

  <ul>
    <% organizations.forEach(organization => { %>
      <li>
        <a href="/organization/<%= organization.organization_id %>">
          <%= organization.name %>
        </a>
      </li>
    <% }) %>
  </ul>
</main>

<%- include('partials/footer') %>